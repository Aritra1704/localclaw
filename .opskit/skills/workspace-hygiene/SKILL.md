# Workspace Hygiene

## Workflow

1. Ensure `.gitignore` contains the LocalClaw baseline ignore rules.
2. Remove `.DS_Store`, `._*`, `.Spotlight-V100`, and `.Trashes` before publish.
3. Verify the publish candidate set is clean before committing.
